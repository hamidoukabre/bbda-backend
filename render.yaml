services:
  - type: web
    name: bbda-backend
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y portaudio19-dev python3-pyaudio
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: "python main_fastapi.py"