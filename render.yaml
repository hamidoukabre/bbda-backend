services:
  - type: web
    name: bbda-backend
    env: python
    buildCommand: |
      rm -f pyproject.toml poetry.lock 2>/dev/null || true
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: "python main_fastapi.py"